## Notebooks folder

**⚠️⚠️ ATTENTION ⚠️⚠️**

> Мы ведем разработку **исключительно** в `.py` файлах (ele.s.shеvchenko будет бить)
> Публикуем только законченные эксперименты с **удаленными output'ами**

Тут можно хранить `.ipynb` файлы, в которых:

1. Артефакты неудачных экспов (типа попробовали метод, он нам не точно подходит, жаль тратить время и переводить его в `.py` файлы). Все, что релеватно и может быть релеватным для проекта, оформляется в `.py` файлы в `src`.
1. Небольшие демки потыкаться
